type Concept implements Node {
  id: ID!
  name: String
  path: [String]
  summary: String
  conceptsCount: Int
  reqs: [Concept]
  concepts(id: String): [Concept]
}

interface Node {
  id: ID!
}

type RootQuery {
  viewer: User
  node(id: ID!): Node
}

type User {
  conceptRoot: Concept
  concept(id: String, path: String): Concept
}
